---
"@parity/truapi": patch
---

Run CLI product scripts in a sandboxed browser by default, enforce remote permissions for requests and redirects, and package the matching browser driver and script compiler. Add explicit browser installation and keep host diagnostics available through `TRUAPI_SCRIPT_MODE=trusted`.
